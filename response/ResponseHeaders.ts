/** Default HTTP header map containing the content type for JSON responses. */
export const JSON_CONTENT_TYPE_HEADER = {
   /** Content-type entry. */
   'content-type': 'application/json; charset=UTF-8',
}
